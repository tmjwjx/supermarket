server:
  http:
    addr: 0.0.0.0:8002
    timeout_ms: 1000
  grpc:
    addr: 0.0.0.0:9002
    timeout_ms: 1000
data:
  database:
    driver: mysql
    source: ${INVENTORY_DATABASE_SOURCE:root:root@tcp(127.0.0.1:3306)/inventory?timeout=5s&parseTime=True&loc=Local&charset=utf8mb4}
    debug: true
    auto_migrate: true
auth:
  jwt_secret: ${AUTH_JWT_SECRET:change-me-local-jwt-secret}
  admin_jwt_secret: ${ADMIN_JWT_SECRET:change-me-admin-jwt-secret}
