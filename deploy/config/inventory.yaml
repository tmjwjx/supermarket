server:
  http:
    addr: 0.0.0.0:8002
    timeout_ms: 1000
  grpc:
    addr: 0.0.0.0:9002
    timeout_ms: 1000
data:
  database:
    driver: mysql
    source: "root:root@tcp(mysql:3306)/inventory?timeout=5s&parseTime=True&loc=Local&charset=utf8mb4"
    debug: true
    auto_migrate: true
auth:
  jwt_secret: tmjwjx-user-jwt-secret
  admin_jwt_secret: tmjwjx-admin-jwt-secret
kafka:
  brokers: kafka:29092
