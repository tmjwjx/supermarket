server {
    listen 5173;
    server_name localhost;
    root /usr/share/nginx/html;
    index index.html;

    # 浏览器走相对路径 /v1 时转到 Compose 网络里的 gateway
    location /v1/ {
        proxy_pass http://gateway:8080;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}
